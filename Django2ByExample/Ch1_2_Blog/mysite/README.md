## Simple blog application from book

### Run server with:
* `pip3 install -r requirements.txt`
* `python3 manage.py runserver`


### Run db on mac with:
* `brew services start postgresql`
* See `mysite/settings.py` for configuration
